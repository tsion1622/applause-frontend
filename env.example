# =================================================================
# == Vercel Environment Variables for Applaude Frontend          ==
# =================================================================
# These variables must be set in the Vercel project settings.
# For production, use Production variables.
# For preview/development branches, use Preview/Development variables.

# --- Public Variables (prefixed with VITE_) ---

# The base URL of your Django backend API.
# This should NOT end with a slash.
# Example for production: https://api.applaude.ai
VITE_API_BASE_URL=https://your-production-api-domain.com

# Sentry DSN for frontend error monitoring.
# This is safe to expose in the frontend code.
VITE_SENTRY_DSN=https://your-sentry-dsn-key@o0.ingest.sentry.io/0

# Optional: Google Analytics Tracking ID
VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# =================================================================
# Note: Any secret keys (e.g., private API keys for third-party services)
# should NOT be prefixed with VITE_ and should only be used in serverless
# functions if needed. They will not be exposed to the browser.
# =================================================================
